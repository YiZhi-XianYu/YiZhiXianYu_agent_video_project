# Asset Domain

本模块管理用户素材和系统生成 Artifact 的元数据、权限及数据血缘。

## 职责

- 管理视频、音频、图片、字幕和中间产物描述。
- 记录存储 URI、内容哈希、媒体类型、大小和生命周期。
- 创建上传会话并校验项目级访问权限。
- 追踪 Artifact 的生产 Task 和上游输入。

## 边界

大型二进制存入对象存储；本模块不在数据库保存媒体正文，也不执行媒体处理。

