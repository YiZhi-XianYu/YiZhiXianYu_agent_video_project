# 项目现状与上手指北

> 当前进度：第七阶段已经完成，人工编辑、版本管理与自然语言时长输入已实现

> 注意：
> .vscode\settings.json 中包含本机绝对路径（Python 解释器、JDK），已在 .gitignore 中加入 .vscode/，不会被提交。如需使用 VSCode，请根据本机环境修改或删除该文件。

## 1. 这是什么项目

这是一个“让系统协助完成视频制作”的项目。

用户把多个长视频交给系统后，系统会自动完成素材检查、生成便于处理的副本、寻找镜头、评价镜头质量、挑选精彩片段，并整理出一份有先后顺序的剪辑安排。

可以把它理解成一位不会直接乱剪视频的“智能剪辑助理”：它先整理素材、给镜头打分、说明选择理由，再交出一张清楚的剪辑清单。以后接入大语言模型时，模型也只能在规定范围内提出建议，不能直接执行电脑命令。

## 2. 一句话说明当前进度

项目已经完成了从”上传多段视频”到”得到一份可编辑的剪辑安排并渲染为最终 MP4 成片”的完整链路。用户可以手动调整每个 Story Beat 中的 shot 分配，保存多版本方案，对比版本差异，一键渲染成片。

因此，可以认为：

- 第一至第七阶段的主干已经搭好；
- 第七阶段已经收尾；
- 下一步可继续推进 LLM 优化、音频/字幕/转场、基础设施完善；
- 最终成片已可渲染，但字幕、音乐、转场效果等仍需后续实现。

## 3. 用户现在已经能做什么

目前，用户可以在浏览器页面中完成这些操作：

1. 创建新项目，或者从数据库中选择以前创建的项目；
2. 在历史项目中继续使用原有素材，不必每次重新上传；
3. 一次上传多个视频，并按所选清晰度启动分析；
4. 输入自然语言成片时长（如"快节奏15秒"、"1分钟慢旅行"）；
5. 查看每个素材的处理进度和每项任务的状态；
6. 查看系统找出的镜头、关键画面、质量评分和 CLIP 语义标签（场景/物体/人物）；
7. 查看镜头排名，以及镜头入选或落选的原因；
8. 查看五段式故事安排和最终的时间线；
9. 查看 LLM 审计面板（AI 生成 vs 确定性算法、Provider、延迟、校验错误）；
10. **编辑 Story Plan**：替换 shot、上下排序、锁定保护、添加镜头、删除镜头；
11. **版本管理**：保存多版本（自定义版本名）、Diff 对比、切换加载、回退恢复、删除；
12. **一键 Apply & Render**：应用编辑后的方案，渲染为最终 MP4 成片并下载；
13. 打开历史运行记录，恢复当时的素材、任务、评分和时间线结果。

系统目前采用的故事顺序是（目前固定，后续接入大模型可扩展为自定义）：

```text
吸引注意 -> 简单介绍 -> 展开旅程 -> 到达高潮 -> 收束结尾
```

在程序内部，这五段分别叫作 `HOOK`、`INTRO`、`JOURNEY`、`CLIMAX` 和 `ENDING`。

## 4. 系统是怎样工作的

为了方便理解，可以把整个过程想成一条分工明确的流水线。

### 第一步：接收和整理素材

系统接收用户选择的多个视频，记录它们属于哪个项目，并保留素材之间的顺序。

### 第二步：每个视频分别分析

每个视频都会独立完成以下工作：

- 读取时长、分辨率、帧率等基本信息；
- 生成一个更适合预览和分析的视频副本；
- 把长视频拆分成多个镜头；
- 为镜头生成关键画面；
- 从清晰度、曝光、稳定程度、画面构图和运动情况等方面评价镜头。

多个素材可以同时处理，不必一个完全结束后再处理下一个。

### 第三步：把所有素材放在一起比较

系统汇总全部镜头后，会排除过短、过糊、曝光明显异常的镜头，再综合考虑：

- 画面本身是否可用；
- 时长是否适合剪辑；
- 是否与已经选中的画面过于相似；
- 是否过多使用了同一个素材；
- 同一素材中的镜头是否离得太近。

排名结果不仅有分数，也会保留扣分项和选择理由，方便成员检查，而不是只给出一个无法解释的结果。

### 第四步：生成故事安排和时间线

系统从合格镜头中选择片段，组成固定的五段式故事，并把各片段放入一条连续的时间线中。当前真实验证使用的是 30 秒目标时长，时间线会精确填满 30 秒。后续接入大模型之后会灵活生成故事安排和时间线。

这里得到的是一份“结构化剪辑单”，还不是最终视频文件。后续的渲染模块会按照这份剪辑单安全地生成成片。

## 5. 已经搭好的项目骨架

项目现在已经具备以下基础能力。

### 多素材处理

一个工作流程可以关联多个视频。系统会为每个素材展开独立任务，再在后面统一汇总，不再局限于一次分析一个视频。

### 清楚的任务先后关系

系统知道哪些任务可以同时开始、哪些任务必须等待前一步完成。如果前面的必要任务失败，后面的任务不会继续生成错误结果。

### Java 与 Python 分工

- Java 负责项目、流程、任务状态、数据库记录和网页接口；
- Python 负责视频读取、镜头分析、质量评分、排名和时间线生成；
- 两边通过 HTTP 接口通信，互相不直接运行对方的代码。

这种分工便于以后分别升级，也能避免把整个项目绑死在一种语言上。

### 结果可追溯

每次产生的素材信息、视频副本、镜头列表、评分、排名、故事安排和时间线都会作为独立结果保存。已经保存的结果不会被原地覆盖，并且能查到它来自哪个项目、素材和任务。

### 历史项目恢复

用户可以读取数据库中的历史项目和历史运行记录。页面能够恢复当时的素材、任务、镜头、评分、故事和时间线，不需要每次新建项目。

### 失败收尾和有限重试

系统已经能够处理一部分临时故障：

- 服务短暂不可用时，任务会等待后重试；
- 重试次数有上限，不会无限循环；
- 等待时间会逐步增加，避免持续挤压服务；
- 定时检查会找回卡在准备、发送或等待重试状态的任务；
- 无法恢复的任务会明确失败，并让整个流程正常收尾，不会永久显示为运行中；
- 页面会显示任务尝试次数，便于排查问题。

### 为大语言模型预留安全边界

项目已经准备了大语言模型生成“故事建议”时必须遵守的数据格式和检查规则。模型以后只能从系统提供的候选镜头中选择，不能生成 Shell、FFmpeg、数据库语句、本地路径或任意工具调用。

## 6. 当前真实验证情况

第七阶段已经使用两段云南旅游长视频完成真实测试：

- 2 个视频素材；
- 18 个任务（2×7 ASSET + 4 WORKFLOW = 18）全部成功；
- 共识别 257 个镜头，CLIP 为每个镜头输出场景/物体/人物语义标签；
- 219 个镜头通过基本筛选，38 个被拒绝；
- LLM（DeepSeek）辅助 Story Plan shot-to-beat 选择，失败时自动 fallback 确定性算法；
- 生成 5 段故事（HOOK→INTRO→JOURNEY→CLIMAX→ENDING）和完整 30 秒 Timeline；
- 前端编辑模式：替换 shot、排序、锁定、添加、删除均正常工作；
- 版本管理：Save Plan（命名版本）、Version List、Diff 对比、Load、Restore、Delete 均验证通过；
- Apply & Render：自定义方案渲染为 MP4 成片，可下载；
- Java 自动化测试 14 项通过；
- Python 自动化测试 35 项通过（2 预存失败，无新增）。

此外，还做过一次“故意关闭视频处理服务，再重新启动”的恢复测试。任务先进入等待重试，服务恢复后整个流程最终成功，说明第四阶段的自动恢复机制已经能够应对基本的临时故障。

这些结果说明项目主链路已经能够真实运行，而不只是停留在页面演示或伪造数据阶段。

## 7. 第一次接触项目时，建议怎样读文档

不要一开始就阅读最厚的系统设计文档。推荐按下面的顺序阅读。

### 第 1 份：本文件

先通过本文件了解项目在做什么、已经走到哪里、接下来还缺什么。

### 第 2 份：`README.md`

用于认识项目目录、当前功能和最基本的启动方式。它更像项目首页，不负责讲完所有设计细节。

### 第 3 至第 8 份：六个阶段文档
>也可以交给ai读,主要用来记录项目进度

按开发先后阅读，最容易看懂项目是怎样一步步长出来的：

1. `docs/first-vertical-slice.md`：最小链路是怎样跑通的；
2. `docs/second-vertical-slice.md`：单个视频怎样生成代理视频；
3. `docs/third-stage-handoff.md`：怎样扩展到多个素材和镜头分析；
4. `docs/fourth-stage-handoff.md`：怎样完成评分、排名、故事安排、时间线、历史恢复和失败重试；
5. `docs/fifth-stage-handoff.md`：怎样安全接入 LLM（DeepSeek）+ 前端展示完善；
6. `docs/sixth-stage-handoff.md`：怎样接入 CLIP 语义理解（场景/物体/人物检测）；
7. `docs/seventh-stage-handoff.md`：人工编辑 Shot、版本管理、自然语言时长输入。

如果只是接手当前代码，可以优先精读第七阶段文档，再按需要回看前六阶段。

### 第 7 份：系统设计文档
>给ai读吧，这个主要是用来约束ai避免跑偏的

`docs/Agent-Driven智能视频制作流水线-系统设计文档.md`

读完阶段文档后，再用这份文档了解项目的整体方向、模块边界和长期设想。它包含的范围比当前已实现功能更大，所以阅读时要注意区分“已经完成”和“未来计划”。

### 第 8 份：Java 控制端模块说明
>建议给ai读

`docs/modules/control-plane/README.md`

准备修改 Java 代码、数据库记录、任务调度或接口时，再阅读这份文档。

### 按工作内容补充（给ai）阅读

- 修改 Java：重点看 `control-plane/` 和控制端模块说明；
- 修改 Python 视频分析：重点看 `tool-service/`；
- 修改双方传递的数据：重点看 `contracts/`；
- 修改启动和测试方式：重点看 `scripts/`；
- 修改页面：页面目前放在 Java 控制端的静态资源中。

## 8. 项目目录地图

```text
control-plane/   Java 控制端、数据库操作、任务调度和当前网页（含 Edit Mode + Version Management）
tool-service/    Python 视频处理、镜头分析、评分、排名、CLIP 语义标签和时间线工具
contracts/       Java 与 Python 共同遵守的数据格式
docs/            设计说明、阶段交接和模块文档
scripts/         本地启动、测试和数据库初始化脚本
```

如果不知道一个功能应该放在哪里，可以先判断它属于“流程管理”还是“视频处理”：前者通常放在 `control-plane/`，后者通常放在 `tool-service/`。两边都需要理解的数据格式应放在 `contracts/`。

## 9. 第九阶段重构已完成（2026-07-26）

第九阶段已成功完成前端全量重写（Vue 3 + TypeScript + Vite）和 Java Workflow 引擎的 Gate 人在回路系统。

### 已完成

- **前端全量重写**：原生 JS → Vue 3 + TypeScript + Pinia + Vue Router + Tailwind CSS
- **Workflow Gate 系统**：RUNNING → PAUSED 暂停机制，5 个 Gate 审核关卡
- **字幕后置流程**：字幕生成从主 DAG 移除，改为成片渲染后 ASR → 二次烧录
- **5 个审核视图**：ShotRanking / StoryEditor / TimelinePreview / FinalReview / FinalDownload
- **版本管理 + LLM 审计**：VersionList / VersionDiff / LlmAuditPanel
- **产品化打磨**：骨架屏加载态、键盘快捷键、移动端响应式、空状态引导

### 重构方案详见

- `docs/ninth-stage-refactoring-plan.md`：完整重构方案设计文档

### 新前端技术栈

- Vue 3.5 + Composition API + `<script setup>`
- TypeScript 5.6 严格模式
- Vite 6 构建工具
- Pinia 状态管理（按 Feature 拆分 Store）
- Vue Router 4
- Tailwind CSS 3 + 自定义颜色系统
- Lucide Vue 图标库

## 10. 还没有实现的功能

以下功能已在第七阶段完成，不再属于待办：

- ~~大语言模型接入~~ → 第五阶段已接入 DeepSeek，含三层校验 + 自动 fallback
- ~~画面内容理解~~ → 第六阶段已接入 CLIP ViT-B-32：场景分类（15 类）、物体检测（15 类）、人物检测（11 类）
- ~~最终成片渲染~~ → 视频渲染已实现（`video.render` Tool + Java TimelineComposer）
- ~~人工编辑与方案版本管理~~ → 第七阶段 P2/P3 已实现

下面是仍未完成的主要待办：

### 1. LLM 采纳率仍需优化

DeepSeek V3 的数值推理能力有限，LLM Story Proposal 采纳率约 1/8（大部分运行走确定性 fallback）。可评估支持 strict structured output 的模型（如 gpt-4o、claude-sonnet-4-6）。

### 2. 排名质量仍需要人工样本校准

当前排名规则可以解释，也已经能过滤明显低质量和重复镜头，但权重仍来自工程经验。需要由团队挑选一批代表性镜头，人工标记”好不好、适不适合做精彩片段”，再据此调整评分权重和门槛。

### 3. 音频和包装能力还没有完成

当前只有一条视频轨道和 CUT 转场，尚未加入：

- 背景音乐和原声处理；
- 字幕和语音识别（ASR）；
- 标题卡和片尾；
- FADE/CROSS_DISSOLVE 转场；
- 调色和画幅适配；
- 音量平衡和节奏配合。

### 4. 运行管理还可以继续完善

自动重试已经具备，但仍缺少页面上的手动重试、取消任务、调整优先级和管理员恢复操作。

### 5. 基础设施工程化

- 数据库迁移：Hibernate `ddl-auto=update` 需替换为 Flyway；
- 任务队列：内存队列需替换为 Celery/Redis；
- 部署：需提供 Docker Compose 一键部署。

### 6. 页面需要继续产品化

当前页面能够完成流程验证，但长视频会产生大量镜头卡片。后续可增加分页、条件筛选、更好的视频预览和更清晰的错误处理。

## 10. 下一阶段最合适做什么

第八阶段建议按以下优先级推进：

1. **P3 — LLM 优化**：评估支持 strict structured output 的模型，改善 duration 求和约束，提高 LLM 采纳率；
2. **P4 — 音频/字幕/转场**：BGM 选择、ASR 语音转写生成字幕、FADE/CROSS_DISSOLVE 转场；
3. **P5 — 基础设施完善**：Flyway 数据库迁移、Celery/Redis 分布式任务队列、Docker Compose 一键部署；
4. **优化项**：人物检测精度提升（YOLO/ViT-L-14）、智能关键帧选择、CLIP 推理合并。

仍必须遵守的安全边界：

```text
LLM 不生成 Shell
LLM 不生成 FFmpeg
LLM 不选择未注册 Tool
LLM 不提供本地路径或 Artifact URI
LLM 不绕过 Ranking/Story/Timeline Validator
LLM 输出失败时使用确定性 fallback
Renderer 只编译已验证的声明式 Timeline IR
Renderer 不接受任意 Shell/FFmpeg 参数
```

## 11. 可以从哪里开始贡献

不建议直接改动整个流程。可以从一个边界清楚的小任务开始：

- 前端方向：为历史运行记录增加筛选，或改善镜头列表的显示性能；
- Java 方向：增加手动重试、取消接口，或完善任务错误展示；
- Python 方向：改进运动和稳定性判断，或增加一种可验证的镜头标签，也可以编写视频处理脚本；
- 测试方向：补充损坏视频、极短视频、无音轨视频和服务中断测试；
- 算法方向：建立人工标记样本，比较系统排名与人工选择的差异；
- 文档方向：为新接口、数据格式和故障处理补充例子。

每次修改前，先确认改动属于哪个阶段和模块；修改后要进行测试。如果改变了 Java 与 Python 之间的数据格式，两边和 `contracts/` 必须一起更新。

## 12. 必须共同遵守的边界

以下规则是项目能够长期维护的基础：

1. Java 和 Python 继续通过 HTTP 工具接口协作，不直接调用对方内部代码；
2. 已经生成并保存的结果不可原地修改，新结果应形成新记录并保留来源；
3. 大语言模型不能生成或执行 Shell、FFmpeg、SQL、本地路径和任意工具参数；
4. 模型输出必须先经过程序检查，失败时必须有固定规则作为备用方案；
5. 密码、API Key 和本地凭据不得写入代码、配置示例、测试、日志或文档；
6. 修改代码前先检查 Git 状态，不覆盖其他成员尚未提交的工作；
7. 每次修改结束时更新交接文档，并用真实多素材进行一次完整验证。
8. 更新文档非常重要！
9. 一定要记得写文档，可以让ai帮忙写！

## 13. 倒数第二个说明

这个项目现在最重要的成果，不是已经做出一个功能齐全的视频剪辑软件，而是已经把可靠的主干搭了起来：素材怎样进入、任务怎样拆分、不同语言怎样合作、结果怎样保存、失败怎样收尾、镜头怎样被选择，以及未来的大语言模型能做什么和不能做什么，都已经有了清楚边界。

接下来的工作应在这套骨架上逐步增加理解画面、提出故事建议和生成成片的能力。只要继续保持“结果可检查、过程可追溯、失败可恢复、模型权限受限制”，团队就可以在不推翻前四阶段成果的情况下持续扩展项目。

## 14. 最后说明

说明一下技术栈：

>ai说啥就是啥吧

当前项目实际已经使用的技术栈如下。这里区分了“已经使用”和“后续计划”，避免把尚未接入的技术算进去。

### 1. 前端

- 原生 HTML5
- 原生 CSS3
- 原生 JavaScript
- Fetch API：调用 Java 后端接口
- FormData：批量上传视频
- 浏览器原生视频播放能力
- 静态资源由 Spring Boot 直接提供

目前没有使用：

- Vue
- React
- Angular
- TypeScript
- Node.js/npm
- Webpack、Vite 等前端构建工具
- 专门的前端组件库

也就是说，目前前端是一个轻量、无构建步骤的静态页面。

### 2. Java 控制端

- Java 21
- Spring Boot 3.4.4
- Spring Web：提供 REST API 和静态页面
- Spring Data JPA：数据库访问
- Hibernate ORM：实体与数据库表映射
- Jakarta Persistence：实体、事务和悲观锁相关注解
- Jakarta Validation：接口参数校验
- Spring Transaction：事务管理
- Spring Scheduling：定时轮询和故障补偿扫描
- Spring Async：异步派发任务
- Java HttpClient：通过 HTTP 调用 Python Tool Service
- Jackson：JSON 序列化、反序列化和 Workflow 快照保存
- Maven：Java 依赖管理、构建和测试
- 内嵌 Tomcat：运行 Spring Boot Web 服务

Java 控制端主要负责：

- 项目与历史项目管理
- 素材记录
- WorkflowDefinition
- DAG 校验和任务展开
- Task 状态管理
- Tool 调用与回调
- 自动重试和失败收尾
- Artifact 元数据与血缘
- 历史 Workflow 恢复
- 浏览器 API

### 3. Python Tool Service

- Python 3.12
- FastAPI 0.116.1
- Uvicorn 0.35.0
- Pydantic
- Pydantic Settings 2.10.1
- HTTPX 0.28.1
- Python `ThreadPoolExecutor`：后台执行视频处理任务
- Python `subprocess`：调用 FFmpeg 和 FFprobe
- Python 标准库：JSON、哈希、路径处理、线程、时间、UUID 等
- Conda：Python 环境管理
- pip：Python 包管理

Python 服务负责：

- Tool 注册和 Tool Manifest
- 异步 Tool Execution
- 幂等执行
- 状态查询和 Java 回调
- 视频信息读取
- 代理视频生成
- 镜头检测
- 关键帧提取
- 镜头质量评分
- 跨素材排名
- Story Plan
- Highlight Selection
- Timeline 生成与校验

当前没有使用 OpenCV、NumPy、PyTorch、TensorFlow 或其他深度学习框架。现有视觉评分主要通过 FFmpeg 抽帧和纯 Python 数学计算实现。

### 4. 视频处理

- FFmpeg
- FFprobe
- H.264 视频编码
- AAC 音频编码
- FFmpeg 场景变化检测
- FFmpeg 关键帧抽取
- FFmpeg 代理视频转码
- FFmpeg 进度输出解析

目前 FFmpeg 只是由受控的 Python Tool 调用，并不是让 LLM 生成 FFmpeg 命令。

### 5. 数据库与持久化

- MySQL
- MySQL Connector/J
- Spring Data JPA
- Hibernate
- 数据库事务
- `PESSIMISTIC_WRITE` 悲观写锁
- 关系表保存 Workflow 与素材集合
- 关系表保存 Task 依赖边
- 本地文件系统保存视频和 Artifact 内容
- `file://` 形式的本地 Artifact 地址
- SHA 哈希：内容校验、血缘和视觉指纹相关处理

数据库保存的核心内容包括：

- Project
- Asset
- WorkflowRun
- Workflow 与 Asset 的关联
- TaskRun
- Task Dependency
- Tool Execution
- Artifact

当前使用 Hibernate `ddl-auto=update` 更新表结构，还没有接入 Flyway 或 Liquibase。

### 6. 服务通信

- RESTful HTTP API
- JSON
- Java 通过 HTTP 调用 Python
- Python通过 HTTP 回调 Java
- Java 定时轮询补偿丢失的回调
- HTTP/1.1
- 异步任务提交
- 幂等键
- 有限重试
- 指数退避
- Tool Manifest 和工具白名单

Java 和 Python 是两个独立服务：

```text
浏览器
   -> Java Spring Boot（8080）
      -> Python FastAPI（8090）
         -> FFmpeg / FFprobe
```

### 7. Workflow 与任务调度

这部分主要是项目自行实现的，不依赖 Temporal、Airflow 或 Camunda：

- WorkflowDefinition
- 有向无环任务图（DAG）
- DAG 拓扑校验
- ASSET 级任务与 WORKFLOW 级任务
- Task 依赖表
- Task 状态机
- 异步任务派发
- Workflow 进度计算
- 上游失败后的下游跳过
- `RETRY_WAIT` 持久化重试状态
- 指数退避
- 幂等执行
- 定时补偿扫描
- Tool Execution 丢失检测
- Workflow 终态收敛
- 数据库悲观锁并发保护

目前没有使用：

- Apache Airflow
- Temporal
- Camunda
- Quartz
- Kafka
- RabbitMQ
- Redis

### 8. 数据协议与安全契约

- OpenAPI 3.1
- JSON Schema Draft 2020-12
- Java Record/DTO
- Pydantic Model
- WorkflowDefinition Schema
- Story Plan Schema
- Timeline Schema
- LLM Story Proposal Schema
- Tool 名称与版本白名单
- 参数白名单
- Timeline 语义校验
- LLM 输出语义校验

这些协议放在 `contracts/`，用于约束 Java、Python以及未来大语言模型之间的数据格式。

### 9. 镜头评分与选择算法

当前属于确定性、可重复运行的传统计算方法：

- Laplacian 方差思想的清晰度评价
- 灰度亮度与曝光评价
- 相邻采样帧差异
- 稳定程度评价
- 构图亮度平衡
- 运动兴趣度
- 64 位灰度视觉指纹
- 汉明距离/指纹相似度
- 时长适配评分
- 质量阈值过滤
- 近重复镜头惩罚
- 素材使用平衡
- 同素材时间邻近惩罚
- 类 MMR 的多样性排名
- 固定五段式 Story Template
- Timeline 连续性和时长校验

当前算法不是机器学习模型，也没有训练过程。

### 10. 测试技术

Java：

- JUnit 5
- Spring Boot Test
- AssertJ
- Maven Test

Python：

- pytest 8.4.1

当前覆盖的测试内容包括：

- WorkflowDefinition 和 DAG 校验
- Task 状态机
- 重试次数与重试状态
- Tool HTTP 数据契约
- 视频 Probe
- 代理视频参数
- Shot Detection
- 质量评分
- Ranking
- Story Plan
- Highlight
- Timeline
- LLM Proposal Validator

### 11. 本地开发与运行环境

- Windows
- PowerShell
- Windows CMD 启动脚本
- IntelliJ IDEA 自带 Maven
- Anaconda/Conda Python 环境
- 本机 MySQL
- 本机文件系统
- Java 服务端口 `8080`
- Python 服务端口 `8090`

目前没有使用：

- Docker
- Docker Compose
- Kubernetes
- 云对象存储
- CI/CD 平台配置
- Nginx
- 微服务注册中心

### 12. 大语言模型现状

当前已正式接入 DeepSeek API（`deepseek-chat`），用于：

- Story Plan 的 shot-to-beat 选择（第五阶段）
- 自然语言时长解析（第七阶段 P1）

已具备的能力：

- 可替换的 LLM Provider 抽象层（DeepSeekProvider / NoopProvider）
- 版本化的 Prompt 模板（v1.1）+ System Prompt SHA-256 哈希追踪
- 三层安全校验：Schema 合约 → 语义 Validator → 编译后 Validator
- 校验失败自动回退确定性算法，用户无感知
- LlmAuditRecord 全程审计（Provider、Model、延迟、校验错误、最终来源）
- 前端 LLM 审计面板（展示 AI 生成 vs 确定性算法）

已知限制：DeepSeek V3 数值推理能力有限，LLM 采纳率约 1/8，大部分运行走确定性 fallback。

API Key 通过 `tool-service/.env` 中的 `TOOL_SERVICE_LLM_API_KEY` 配置，不配置时自动使用 NoopProvider。

核心版本来源可查看：

- [control-plane/pom.xml](./control-plane/pom.xml)
- [tool-service/requirements.txt](./tool-service/requirements.txt)
- [tool-service/environment.yml](./tool-service/environment.yml)
- [application.yml](./control-plane/src/main/resources/application.yml)