# Context Domain

本模块管理 Agent 与 Workflow 使用的分层结构化上下文。

## 职责

- 管理用户偏好、项目、工作流、任务和会话上下文。
- 保存 Artifact 引用、预算、约束、假设和摘要。
- 为 Planner 与 Task 提供最小必要上下文视图。
- 控制上下文版本、权限和大小。

## 边界

不重复保存大型转写、帧数组或视频数据；这些内容只通过 Artifact 引用访问。

