# API 文档

本目录描述系统对外 REST API、WebSocket 事件和内部 Tool 调用协议。

## 职责

- 解释接口用途、调用顺序、鉴权方式和错误语义。
- 保存面向开发者的请求与响应示例。
- 记录 API 版本兼容和废弃策略。

## 边界

机器可校验的 OpenAPI 与 JSON Schema 放在 `contracts/`；本目录只保存便于阅读的说明和使用指南。

