# 部署与运行环境

本目录负责描述和组织本地开发、演示及未来生产环境的部署配置。

## 职责

- 编排 Web、Java 控制面、Python Tool Service、Worker、MySQL、Redis 和 MinIO。
- 管理环境变量模板、端口、网络、数据卷和健康检查。
- 区分 CPU 与可选 GPU Worker 的运行方式。

## 边界

不在此目录保存密码、访问密钥、模型文件或用户视频。真实敏感配置应由本地环境或密钥管理系统注入。

